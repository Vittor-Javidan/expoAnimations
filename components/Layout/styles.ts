import { StyleSheet } from "react-native";

export function getStyles() {
    return StyleSheet.create({
        mainContainer: {
            flex: 1,
            backgroundColor: "#000"
        },
    })
}
